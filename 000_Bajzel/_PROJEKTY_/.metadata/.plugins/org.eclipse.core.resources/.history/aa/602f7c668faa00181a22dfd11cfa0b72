#include <Arduino.h>

#define PINLED 13
int inter;
uint8_t dzban;
void setup()
{
	pinMode(PINLED, OUTPUT);
	inter = millis();
}



void loop()
{
	digitalWrite(PINLED, !digitalRead(PINLED));
	delay(250);
	test();}
void test()
{
	int howMany = 10;
	for (inter = 0; inter < howMany; ++inter)
	{

	}
	int limit = 30;
	for (inter = 0; inter < limit; ++inter)
	{

	}
}

void noc()
{

}
